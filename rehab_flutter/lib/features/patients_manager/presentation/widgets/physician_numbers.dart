import 'package:flutter/material.dart';

class PhysicianNumbers extends StatelessWidget {
  const PhysicianNumbers({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
