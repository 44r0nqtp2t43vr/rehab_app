import 'package:flutter/widgets.dart';

class MTScreenPlaylist extends StatelessWidget {
  const MTScreenPlaylist({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
